===== SKIPPED (binary) ./.DS_Store =====

===== ./BG.html =====
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <title>随机旋转平铺背景 + WASD or 自动环形移动</title>
  <style>
    /* 全屏黑色背景，无滚动条 */
    html, body {
      margin: 0;
      padding: 0;
      overflow: hidden;
      background: #000;
      height: 100%;
    }
    /* 画布无边框，块级显示 */
    #gameCanvas {
      display: block;
      background: #000;
      border: none;
    }
  </style>
</head>
<body>
  <canvas id="gameCanvas"></canvas>

  <script>
    const canvas = document.getElementById('gameCanvas');
    const ctx = canvas.getContext('2d');

    // 使 canvas 始终填满窗口
    function resizeCanvas() {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    }
    window.addEventListener('resize', resizeCanvas);
    resizeCanvas();

    // 检查 URL 参数决定是否自动模式
    const params = new URLSearchParams(window.location.search);
    const AUTO_MODE = params.get('auto') === 'true';

    // 加载贴图
    const img = new Image();
    img.src = 'assets/media/BGelement.png';

    // 随机稳定旋转：0,90,180,270 度
    function getRotationAngle(i, j) {
      const seed = (i * 374761393 + j * 668265263) & 3;
      return [0, 90, 180, 270][seed] * Math.PI / 180;
    }

    // 根据 offset 绘制平铺背景
    function drawBackground(offsetX, offsetY) {
      const tw = img.width, th = img.height;
      const cols = Math.ceil(canvas.width / tw) + 2;
      const rows = Math.ceil(canvas.height / th) + 2;
      const startCol = Math.floor(offsetX / tw);
      const startRow = Math.floor(offsetY / th);

      for (let i = startCol; i < startCol + cols; i++) {
        for (let j = startRow; j < startRow + rows; j++) {
          const x = i * tw - offsetX;
          const y = j * th - offsetY;
          const angle = getRotationAngle(i, j);

          ctx.save();
          ctx.translate(x + tw / 2, y + th / 2);
          ctx.rotate(angle);
          ctx.drawImage(img, -tw / 2, -th / 2);
          ctx.restore();
        }
      }
    }

    // 通用偏移量
    let offsetX = 0, offsetY = 0;

    if (!AUTO_MODE) {
      // —— 键盘控制模式 —— 
      const SPEED = 200;  // px/s
      const keys = {};

      window.addEventListener('keydown', e => {
        keys[e.key.toLowerCase()] = true;
      });
      window.addEventListener('keyup', e => {
        keys[e.key.toLowerCase()] = false;
      });

      let lastTime = 0;
      function loopKeys(timestamp) {
        const dt = (timestamp - lastTime) / 1000;
        lastTime = timestamp;

        // W/↑ 向上，S/↓ 向下，A/← 向左，D/→ 向右
        if (keys['w'] || keys['arrowup'])    offsetY -= SPEED * dt;
        if (keys['s'] || keys['arrowdown'])  offsetY += SPEED * dt;
        if (keys['a'] || keys['arrowleft'])  offsetX -= SPEED * dt;
        if (keys['d'] || keys['arrowright']) offsetX += SPEED * dt;

        ctx.clearRect(0, 0, canvas.width, canvas.height);
        if (img.complete) {
          drawBackground(offsetX, offsetY);
        }
        requestAnimationFrame(loopKeys);
      }

      img.onload = () => {
        requestAnimationFrame(loopKeys);
      };
    } else {
      // —— 自动环形移动模式 —— 
      const AUTO_SPEED = 50;      // px/s
      const ANGULAR_SPEED = 0.05;  // rad/s

      let autoAngle = 0;
      let lastTime = 0;
      function loopAuto(timestamp) {
        const dt = (timestamp - lastTime) / 1000;
        lastTime = timestamp;

        autoAngle += ANGULAR_SPEED * dt;
        // 环形分量
        offsetX += AUTO_SPEED * Math.cos(autoAngle) * dt;
        offsetY += AUTO_SPEED * Math.sin(autoAngle) * dt;

        ctx.clearRect(0, 0, canvas.width, canvas.height);
        if (img.complete) {
          drawBackground(offsetX, offsetY);
        }
        requestAnimationFrame(loopAuto);
      }

      img.onload = () => {
        requestAnimationFrame(loopAuto);
      };
    }
  </script>
</body>
</html>

===== SKIPPED (binary) ./assets/.DS_Store =====

===== SKIPPED (binary) ./assets/font/Minecraft.ttf =====

===== SKIPPED (binary) ./assets/media/BGelement.png =====

===== SKIPPED (binary) ./assets/media/start.png =====

===== SKIPPED (binary) ./assets/media/titled-3.png =====

===== ./css/style.css =====
@font-face {
    font-family: 'Minecraft';
    src: url('../assets/font/Minecraft.ttf') format('truetype');
}

/* Force all elements to use Minecraft font */
* {
    font-family: 'Minecraft', Arial, sans-serif !important;
}

body {
    margin: 0;
    overflow: hidden;
    font-family: 'Minecraft', Arial, sans-serif;
}

canvas {
    display: block;
}

/* Intro Screen */
#introScreen #titleImage {
    position: absolute;
    top: 10%;
    left: 50%;
    transform: translateX(-50%) scale(1.25);
    max-width: 400px;
    mix-blend-mode: lighten;
}
    /* 其余样式保持不变 */
    animation: glow 2s ease-in-out infinite alternate;
  
  @keyframes glow {
    from {
      filter: drop-shadow(0 0 5px rgba(255,255,255,0.6));
    }
    to {
      filter: drop-shadow(0 0 20px rgba(255,255,255,0.9));
    }

}

#introScreen #continueText {
    position: absolute;
    bottom: 10%;             /* 下移到离底部 10% */
    left: 50%;
    transform: translateX(-50%);
    font-size: 28px;
    font-weight: normal;
    color: white;
    text-shadow: 2px 2px 5px rgba(0,0,0,0.7);
    animation: pulse 1.5s ease-in-out infinite; /* 添加放大缩小动画 */
}

@keyframes pulse {
    0%, 100% {
        transform: translateX(-50%) scale(1);
    }
    50% {
        transform: translateX(-50%) scale(1.2);
    }
}

/* Common Menu Styles */
.menuContainer {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 300px;
    background: rgba(0,0,0,0.8);
    color: white;
    text-align: center;
    font-size: 24px;
    padding: 20px;
    border-radius: 8px;
}

.menuButton {
    width: 100%;
    margin: 10px 0;
    padding: 12px 0;
    background: rgba(255,255,255,0.1);
    border: 2px solid #fff;
    border-radius: 4px;
    font-size: 20px;
    color: white;
    cursor: pointer;
    transition: background 0.3s, transform 0.1s;
}

.menuButton:hover {
    background: rgba(255,255,255,0.2);
    transform: scale(1.02);
}

/* Load / Save Lists */
.menuList {
    list-style: none;
    padding: 0;
    margin: 10px 0;
    max-height: 200px;
    overflow-y: auto;
}

.menuList li {
    margin: 5px 0;
    font-size: 18px;
    cursor: pointer;
}

.menuList li:hover {
    text-decoration: underline;
}

/* Load container full-width and scrollable */
.loadContainer {
    position: absolute;
    top: 5%;               /* title nearer to top */
    left: 50%;
    transform: translateX(-50%);
    width: 90%;            /* nearly full width */
    max-height: 90vh;      /* vertical scroll area */
    background: rgba(0, 0, 0, 0.8);
    padding: 20px;
    border-radius: 8px;
    overflow-y: auto;      /* vertical scroll */
}

/* Table wrapper */
.tableContainer {
    width: 100%;
    overflow-y: auto;
}

/* Load Game Table Styles */
.loadTable {
    width: 100%;
    border-collapse: collapse;
}
.loadTable th,
.loadTable td {
    border: 1px solid #fff;
    padding: 8px;
    text-align: center;
    color: #fff;
}
.loadTable th {
    background: rgba(255, 255, 255, 0.1);
}

===== ./export_all.sh =====
#!/usr/bin/env bash

# 输出文件
output="all.txt"

# 如果已经存在，就先清空
> "$output"

# 遍历所有文件，排除 all.txt、GameFile.js，以及任意 node_modules 目录
find . \
  \( \
    -path '*/node_modules/*' \
    -o -name "$(basename "$output")" \
    -o -name "GameFile.js" \
  \) -prune \
  -o -type f -print | sort | while IFS= read -r file; do
  # 用 file 判断是不是文本
  if file "$file" | grep -qE 'text|empty'; then
    printf "===== %s =====\n" "$file" >> "$output"
    cat "$file" >> "$output"
    printf "\n\n" >> "$output"
  else
    printf "===== SKIPPED (binary) %s =====\n\n" "$file" >> "$output"
  fi
done

echo "✅ 已将所有文本文件的路径和内容输出到 $output，二进制文件已被跳过。"

===== ./game.html =====
<!-- project-root/game.html -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>My P5 Game</title>
  <!-- 引入 p5.js -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/1.4.0/p5.min.js"></script>
  <!-- 你的游戏逻辑 -->
  <script src="GameFile.js"></script>
</head>
<body>
  <!-- p5.js 会自动把 canvas 插入到 body 中 -->
</body>
</html>

===== ./index.html =====
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Pixelated Space Demo</title>
  <link rel="stylesheet" href="css/style.css" />
</head>
<body>
  <!-- Three.js canvas 注入点 -->
  <div id="canvas-container"></div>

  <!-- 启动模块化应用 -->
  <script type="module" src="js/app.js"></script>
</body>
</html> 

===== ./js/app.js =====
import { initRouter } from './router.js';
import { initBackground } from './background.js';

document.addEventListener('DOMContentLoaded', () => {
  initRouter();
  initBackground(document.getElementById('canvas-container'));
});

===== ./js/background.js =====
// js/background.js
import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.128.0/build/three.module.js';

let scene, camera, renderer, starGroup;
// 环形转速
const angularSpeed = 0.0005;
// 相机推进参数
const moveDuration = 1;      // 推进时长 (秒)
const moveDistance = 375;    // 推进距离
let moving = false;
let moveStart = 0;
let origCamZ = 0;

// 新增：用于正弦振荡的相位和最大角度（15°转换为弧度）
let oscillationPhase = 0;
const maxAngle = THREE.MathUtils.degToRad(15);

export function initBackground(container) {
  scene    = new THREE.Scene();
  camera   = new THREE.PerspectiveCamera(75, window.innerWidth/window.innerHeight, 0.1, 1000);
  camera.position.set(0, 0, 5);
  camera.lookAt(0, 0, 0);

  renderer = new THREE.WebGLRenderer({ antialias: false });
  renderer.setSize(window.innerWidth, window.innerHeight);
  container.appendChild(renderer.domElement);

  // 星星组
  starGroup = new THREE.Group();
  for (let i = 0; i < 1500; i++) {
    const geo  = new THREE.SphereGeometry(1, 12, 12);
    const mat  = new THREE.MeshBasicMaterial({ color: 0xffffff });
    const star = new THREE.Mesh(geo, mat);
    star.position.set(
      Math.random() * 600 - 300,
      Math.random() * 600 - 300,
      Math.random() * -1200 - 50
    );
    starGroup.add(star);
  }
  scene.add(starGroup);

  window.addEventListener('resize', onWindowResize);
  window.addEventListener('mousemove', onMouseMove);
  animate();
}

// 外部调用此接口触发相机短暂推进
export function startFlight() {
  if (!moving) {
    moving    = true;
    moveStart = performance.now();
    origCamZ  = camera.position.z;
  }
}

function animate() {
  requestAnimationFrame(animate);

  // 星云环形振荡（0～15°往返）
  oscillationPhase += angularSpeed;
  starGroup.rotation.y = (Math.sin(oscillationPhase) + 1) / 2 * maxAngle;

  // 相机短暂推进逻辑
  if (moving) {
    const elapsed = (performance.now() - moveStart) / 1000;
    if (elapsed <= moveDuration) {
      const t = elapsed / moveDuration; // 0→1
      // 缓动推进距离
      const delta = easeInOutQuad(t) * moveDistance;
      camera.position.z = origCamZ - delta;
    } else {
      // 推进结束，恢复位置并触发路由事件
      camera.position.z = origCamZ - moveDistance;
      moving = false;
      window.dispatchEvent(new Event('flightComplete'));
    }
  }

  renderer.render(scene, camera);
}

function onWindowResize() {
  renderer.setSize(window.innerWidth, window.innerHeight);
  camera.aspect = window.innerWidth/window.innerHeight;
  camera.updateProjectionMatrix();
}

function onMouseMove(e) {
  const x     = (e.clientX - window.innerWidth / 2)  / window.innerWidth;
  const y     = (e.clientY - window.innerHeight / 2) / window.innerHeight;
  const range = 20;
  const img   = document.getElementById('titleImage');
  const txt   = document.getElementById('continueText');
  if (img) img.style.transform =
    `translateX(calc(-50% + ${-x*range}px)) translateY(${-y*range}px) scale(1.25)`;
  if (txt) txt.style.transform =
    `translateX(calc(-50% + ${ x*range*1.5 }px)) translateY(${ y*range*1.5 }px)`;
}

function easeInOutQuad(t) {
  return t < 0.5
    ? 2 * t * t
    : -1 + (4 - 2 * t) * t;
}

===== ./js/router.js =====
import { startFlight } from './background.js';
import { IntroScreen } from './ui/introScreen.js';
import { MainMenu    } from './ui/mainMenu.js';
import { LoadMenu    } from './ui/loadMenu.js';
import { SaveMenu    } from './ui/saveMenu.js';
import { ExitConfirm } from './ui/exitConfirm.js';

const modules = {
  INTRO:        IntroScreen,
  MAIN_MENU:    MainMenu,
  LOAD_MENU:    LoadMenu,
  SAVE_MENU:    SaveMenu,
  EXIT_CONFIRM: ExitConfirm
};
let currentState = null;

export function initRouter() {
  switchTo('INTRO');
}

// 直接切 UI
export function switchTo(state) {
  if (currentState) modules[currentState].hide();
  currentState = state;
  modules[state].show();
}

// 触发一次“段落飞行”后再切 UI
export function advanceTo(state) {
  const handler = () => {
    window.removeEventListener('flightComplete', handler);
    switchTo(state);
  };
  window.addEventListener('flightComplete', handler);
  startFlight();
}

===== ./js/supabase.js =====
import { createClient } from '@supabase/supabase-js';

// 初始化 Supabase 客户端
const SUPABASE_URL = 'https://zogcqsdolkkjwnimnbaw.supabase.co';
const SUPABASE_ANON = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpvZ2Nxc2RvbGtranduaW1uYmF3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDYxOTkwMDAsImV4cCI6MjA2MTc3NTAwMH0.OrDcGnhJWUrKGs6oDfJ0Kb9cmxBDggYtmVleAAlnGTo';

export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON);

===== ./js/ui/escMenu.js =====
// js/ui/escMenu.js
import { switchTo } from '../router.js';

export const EscMenu = {
  el: null,

  /**
   * 弹出 Esc 菜单
   * @param {Object} options
   * @param {Function} options.onResume 点击 Resume 的回调
   */
  show({ onResume }) {
    this.el = document.createElement('div');
    this.el.className = 'menuContainer';
    this.el.innerHTML = `
      <h2>Paused</h2>
      <button class="menuButton" id="resumeBtn">Resume Game</button>
      <button class="menuButton" id="saveBtn">Save Game</button>
      <button class="menuButton" id="loadBtn">Load Game</button>
      <button class="menuButton" id="exitBtn">Exit to Main Menu</button>
    `;
    document.body.appendChild(this.el);

    document.getElementById('resumeBtn').onclick = () => {
      this.hide();
      if (onResume) onResume();
    };
    document.getElementById('saveBtn').onclick = () => {
      this.hide();
      switchTo('SAVE_MENU');
    };
    document.getElementById('loadBtn').onclick = () => {
      this.hide();
      switchTo('LOAD_MENU');
    };
    document.getElementById('exitBtn').onclick = () => {
      window.location.href = 'index.html';
    };
  },

  hide() {
    if (this.el) this.el.remove();
  }
};

===== ./js/ui/exitConfirm.js =====
import { switchTo } from '../router.js';

export const ExitConfirm = {
  el: null,
  show() {
    this.el = document.createElement('div');
    this.el.className = 'menuContainer';
    this.el.innerHTML = `
      <h2>Are you sure you want to exit?</h2>
      <button class="menuButton" id="yesExit">Yes</button>
      <button class="menuButton" id="noExit">No</button>
    `;
    document.body.appendChild(this.el);
    document.getElementById('yesExit').onclick = () => window.close();
    document.getElementById('noExit').onclick  = () => switchTo('MAIN_MENU');
  },
  hide() {
    this.el.remove();
  }
};

===== ./js/ui/introScreen.js =====
import { advanceTo } from '../router.js';

export const IntroScreen = {
  el: null,
  show() {
    this.el = document.createElement('div');
    this.el.id = 'introScreen';
    this.el.innerHTML = `
      <img id="titleImage" src="assets/media/titled-3.png" alt="Title Image" />
      <div id="continueText">Press any key to continue</div>
    `;
    document.body.appendChild(this.el);
    window.addEventListener('keydown', this.onKey);
  },
  hide() {
    window.removeEventListener('keydown', this.onKey);
    this.el.remove();
  },
  onKey() {
    // 按任意键，先飞一小段，然后进主菜单
    advanceTo('MAIN_MENU');
  }
};

===== ./js/ui/loadMenu.js =====
import { supabase } from '../supabase.js';
import { advanceTo, switchTo } from '../router.js';

export const LoadMenu = {
  el: null,
  show: async function() {
    this.el = document.createElement('div');
    this.el.className = 'loadContainer';

    // 从 Supabase 拉取存档列表
    let { data: saves, error } = await supabase
      .from('saves')
      .select('*')
      .order('creation_time', { ascending: false });
    if (error) {
      alert('Load failed: ' + error.message);
      saves = [];
    }

    // 构建表格行
    const rows = saves.length ? saves.map(s => {
      const created = new Date(s.creation_time);
      const diffMs = Date.now() - created.getTime();
      const h = Math.floor(diffMs / 3600000);
      const m = Math.floor((diffMs % 3600000) / 60000);
      const playTime = `${h.toString().padStart(2,'0')}:${m.toString().padStart(2,'0')}`;
      return `
        <tr data-id="${s.id}">
          <td>${created.toLocaleString()}</td>
          <td>${s.name}</td>
          <td>${s.current_level}</td>
          <td>${playTime}</td>
          <td>${s.mode}</td>
          <td><button class="menuButton deleteBtn" data-id="${s.id}">Delete</button></td>
        </tr>`;
    }).join('') : `<tr><td colspan="6">(no saves found)</td></tr>`;

    this.el.innerHTML = `
      <h2>Load Game</h2>
      <div class="tableContainer">
        <table class="loadTable">
          <thead>
            <tr><th>Created</th><th>Save Name</th><th>Level</th><th>Playtime</th><th>Mode</th><th>Actions</th></tr>
          </thead>
          <tbody>${rows}</tbody>
        </table>
      </div>
      <button class="menuButton" id="backFromLoad">Back</button>
    `;
    document.body.appendChild(this.el);

    // 点击行加载对应存档
    this.el.querySelectorAll('tbody tr[data-id]').forEach(tr => {
      tr.addEventListener('click', () => {
        const id = tr.getAttribute('data-id');
        window.location.href = `game.html?saveId=${id}`;
      });
    });

    // 删除按钮
    this.el.querySelectorAll('.deleteBtn').forEach(btn => {
      btn.addEventListener('click', async e => {
        e.stopPropagation();
        const id = btn.getAttribute('data-id');
        if (confirm('Are you sure you want to delete this save?')) {
          const { error } = await supabase.from('saves').delete().eq('id', id);
          if (error) return alert('Delete failed: ' + error.message);
          this.hide();
          this.show();
        }
      });
    });

    document.getElementById('backFromLoad').onclick = () => switchTo('MAIN_MENU');
  },
  hide() {
    if (this.el) this.el.remove();
  }
};

===== ./js/ui/mainMenu.js =====
import { advanceTo } from '../router.js';

export const MainMenu = {
  el: null,
  show() {
    this.el = document.createElement('div');
    this.el.className = 'menuContainer';
    this.el.innerHTML = `
      <button class="menuButton" id="loadBtn">Load Game</button>
      <button class="menuButton" id="newBtn">Start New Game</button>
      <button class="menuButton" id="exitBtn">Exit Game</button>
    `;
    document.body.appendChild(this.el);
    document.getElementById('loadBtn').onclick = () => advanceTo('LOAD_MENU');
    document.getElementById('newBtn').onclick  = () => advanceTo('SAVE_MENU');
    document.getElementById('exitBtn').onclick = () => advanceTo('EXIT_CONFIRM');
  },
  hide() {
    this.el.remove();
  }
};

===== ./js/ui/saveMenu.js =====
import { supabase } from '../supabase.js';
import { advanceTo } from '../router.js';

export const SaveMenu = {
  el: null,
  show() {
    this.el = document.createElement('div');
    this.el.className = 'menuContainer';
    this.el.innerHTML = `
      <h2>Create New Save</h2>
      <div class="optionsArea">
        <p>Save Name:</p>
        <input type="text" id="saveNameInput" placeholder="Enter save name" />
      </div>
      <div class="optionsArea">
        <p>Mode:</p>
        <label><input type="radio" name="mode" value="easy" checked> Easy</label>
        <label><input type="radio" name="mode" value="hard"> Hard</label>
      </div>
      <button class="menuButton" id="confirmSave">Save & Play</button>
      <button class="menuButton" id="deleteSave">Delete Save</button>
      <button class="menuButton" id="backFromSave">Back</button>
    `;
    document.body.appendChild(this.el);

    // 保存存档到 Supabase
    document.getElementById('confirmSave').onclick = async () => {
      const nameInput = document.getElementById('saveNameInput');
      const name = nameInput.value.trim();
      if (!name) return alert('Please enter a save name.');
      const mode = document.querySelector('input[name="mode"]:checked').value;
      const { data, error } = await supabase
        .from('saves')
        .insert([{ name, current_level: 1, mode }])
        .select();
      if (error) return alert('Save failed: ' + error.message);
      // 跳转到游戏页面并带上存档 ID
      const saveId = data[0].id;
      window.location.href = `game.html?saveId=${saveId}`;
    };

    // 删除存档（根据名称）
    document.getElementById('deleteSave').onclick = async () => {
      const nameInput = document.getElementById('saveNameInput');
      const name = nameInput.value.trim();
      if (!name) return alert('Please enter the save name you want to delete.');
      const { error } = await supabase.from('saves').delete().eq('name', name);
      if (error) return alert('Delete failed: ' + error.message);
      alert(`Save '${name}' deleted.`);
      nameInput.value = '';
    };

    // 返回主菜单
    document.getElementById('backFromSave').onclick = () => advanceTo('MAIN_MENU');
  },
  hide() {
    if (this.el) this.el.remove();
  }
};

===== SKIPPED (binary) ./package-lock.json =====

===== ./shop.html =====
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Shop Page (Overlay Background)</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      overflow: hidden;
      font-family: sans-serif;
    }
    /* iframe full-screen background */
    #bgFrame {
      position: absolute;
      top: 0; left: 0;
      width: 100vw; height: 100vh;
      border: none;
      z-index: 0;
      pointer-events: none;
    }
    /* shop panel */
    #shopPanel {
      position: absolute;
      top: 20px; right: 20px;
      width: 320px;
      background: rgba(255,255,255,0.95);
      border-radius: 6px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.3);
      padding: 16px;
      z-index: 1;
    }
    #header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 12px;
    }
    #header span { font-weight: bold; }
    #header button {
      padding: 4px 8px;
      cursor: pointer;
    }
    #shop {
      display: flex;
      gap: 8px;
      margin-bottom: 12px;
    }
    .column {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 6px;
      padding: 6px;
      background: #f9f9f9;
      border: 2px solid #ccc;
      border-radius: 4px;
    }
    /* Attribute colors */
    .column.agile    { border-color: blue; }
    .column.tank     { border-color: orange; }
    .column.strength { border-color: red; }
    /* Active highlight */
    .column.agile.active    { box-shadow: 0 0 8px rgba(0,0,255,0.5); }
    .column.tank.active     { box-shadow: 0 0 8px rgba(255,165,0,0.5); }
    .column.strength.active { box-shadow: 0 0 8px rgba(255,0,0,0.5); }
    .item {
      flex: 1;
      padding: 6px;
      text-align: center;
      border: 1px solid #666;
      border-radius: 4px;
      background: #eee;
      cursor: pointer;
      user-select: none;
    }
    .item.disabled {
      background: #ccc;
      color: #777;
      cursor: not-allowed;
    }
    #storedSection h3 {
      margin: 0 0 6px;
      font-size: 1em;
    }
    #storedList {
      list-style: none;
      padding: 0;
      margin: 0;
      font-size: 0.9em;
    }
    #storedList li {
      padding: 2px 0;
      border-bottom: 1px solid #ddd;
    }
    #storedList li:last-child {
      border-bottom: none;
    }
  </style>
</head>
<body>
  <!-- background in iframe -->
  <iframe id="bgFrame" src="BG.html?auto=true"></iframe>

  <!-- shop panel -->
  <div id="shopPanel">
    <div id="header">
      <span id="levelDisplay">Level: 0</span>
      <button id="resetBtn">Reset</button>
    </div>
    <div id="shop">
      <div class="column agile"    data-col="0">
        <div class="item" data-row="0">Agile – Skill 1</div>
        <div class="item" data-row="1">Agile – Skill 2</div>
        <div class="item" data-row="2">Agile – Passive</div>
      </div>
      <div class="column tank"     data-col="1">
        <div class="item" data-row="0">Tank – Skill 1</div>
        <div class="item" data-row="1">Tank – Skill 2</div>
        <div class="item" data-row="2">Tank – Passive</div>
      </div>
      <div class="column strength" data-col="2">
        <div class="item" data-row="0">Strength – Skill 1</div>
        <div class="item" data-row="1">Strength – Skill 2</div>
        <div class="item" data-row="2">Strength – Passive</div>
      </div>
    </div>
    <div id="storedSection">
      <h3>Stored (max 3)</h3>
      <ul id="storedList"><li>None</li></ul>
    </div>
  </div>

  <script>
    // --- state ---
    let level = 0;
    let chosenCol = null;
    const stored = [];
    const MAX = 3;
    const attrNames = ['Agile','Tank','Strength'];

    // --- elements ---
    const lvlDisplay = document.getElementById('levelDisplay');
    const resetBtn    = document.getElementById('resetBtn');
    const shop        = document.getElementById('shop');
    const storedList  = document.getElementById('storedList');

    // --- render stored list ---
    function renderStored() {
      storedList.innerHTML = '';
      if (stored.length === 0) {
        storedList.innerHTML = '<li>None</li>';
      } else {
        stored.forEach(name => {
          const li = document.createElement('li');
          li.textContent = name;
          storedList.appendChild(li);
        });
      }
    }

    // --- update UI for all items ---
    function updateUI() {
      lvlDisplay.textContent = 'Level: ' + level;
      document.querySelectorAll('.column').forEach(colEl => {
        const col = +colEl.dataset.col;
        const isChosen = (col === chosenCol);
        colEl.classList.toggle('active', isChosen);

        colEl.querySelectorAll('.item').forEach(item => {
          const row = +item.dataset.row;
          const name = item.textContent;
          item.classList.remove('disabled');

          // If level 2–4, all items are disabled
          if (level >= 2) {
            return item.classList.add('disabled');
          }

          // 1) already stored or at limit
          if (stored.includes(name) || stored.length >= MAX) {
            return item.classList.add('disabled');
          }

          // 2) Level 0: only row 0 allowed
          if (level === 0) {
            if (row !== 0) {
              return item.classList.add('disabled');
            }
            return;
          }

          // 3) Level 1 logic
          // row2 always disabled for manual
          if (row === 2) {
            return item.classList.add('disabled');
          }

          if (stored.length === 1) {
            // second pick: allow specialization or one other
            if (col !== chosenCol) {
              const anyInThisCol = stored.some(n => n.startsWith(attrNames[col]));
              if (anyInThisCol) {
                return item.classList.add('disabled');
              }
            }
          } else if (stored.length === 2) {
            // third pick only for other attr not yet picked
            if (col === chosenCol) {
              return item.classList.add('disabled');
            }
            const anyInThisCol = stored.some(n => n.startsWith(attrNames[col]));
            if (anyInThisCol) {
              return item.classList.add('disabled');
            }
          }
        });
      });

      renderStored();
    }

    // --- click handler ---
    shop.addEventListener('click', e => {
      const item = e.target.closest('.item');
      if (!item || item.classList.contains('disabled')) return;

      // Prevent clicks at levels 2–4
      if (level >= 2) return;

      const col = +item.parentElement.dataset.col;
      const row = +item.dataset.row;
      const name = item.textContent;

      if (level === 0) {
        // first pick
        stored.push(name);
        chosenCol = col;
        level = 1;
        alert(`Stored: ${name}`);
        updateUI();
        return;
      }

      if (level === 1) {
        if (stored.length === 1 && col === chosenCol) {
          // specialization branch
          stored.push(name);
          // auto-add passive
          const passiveName = `${attrNames[col]} – Passive`;
          stored.push(passiveName);
          alert(`Specialized: ${name} + ${passiveName}`);
          // after specialization, advance to level 2
          level = 2;
          updateUI();
          return;
        }
        if (stored.length < MAX && col !== chosenCol) {
          // other-attribute branch
          stored.push(name);
          alert(`Stored: ${name}`);
          // after third pick, advance to level 2
          if (stored.length === MAX) {
            level = 2;
          }
          updateUI();
          return;
        }
      }
    });

    // --- reset ---
    resetBtn.addEventListener('click', () => {
      level = 0;
      chosenCol = null;
      stored.length = 0;
      updateUI();
    });

    // --- init ---
    updateUI();
  </script>
</body>
</html>

